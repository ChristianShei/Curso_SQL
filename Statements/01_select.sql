SELECT * FROM users; // Me va a mostrar todos los usuarios con sus atributos

SELECT name FROM users; // Me va a mostrar solo los nombes de los usuarios

SELECT user_id, name FROM users;// Me va a mostrar los ID y los nombes de los usuarios